package com.kodilla.stream.beautifier;

public class MethodSource {
    public static String decorateMethod(String string, String bonus) {
        String result = bonus + string + bonus;
        return result;
    }
}
